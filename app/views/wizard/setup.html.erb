<div class="alert alert-info">
	<h2>Get advice on where to live in 2 easy steps!</h2>
</div>

<br/>

<%= form_tag "/wizard/calculate", method: "get" do %>
<h1>1. What college department are you a part of?</h1>
<br/>
<div class="row">
	<div class="span6 offset1">
		<select name="department" class="span5 dropdown">
			<% @departments.each do |department| %>
				<option><%= department.name %></option>
			<% end %>
		</select>
	</div>
</div>

<br/>

<h1>2. Choose your top 3 criteria for this decision. <small>Distribute 100 points between them<small></h1>
<br/>
<div class="row">
	<div class="span5 offset1">
		<select name="criteria[][name]" class="span5 dropdown" prompt>
			<% @criteria.each do |criterium| %>
				<option><%= criterium.name %></option>
			<% end %>
		</select>
	</div>
	<div class="span1"><%= number_field_tag 'criteria[][weight]', 60, :in => 0..100, :class => "number_field" %></div>
</div>

<div class="row">
	<div class="span5 offset1">
		<select name="criteria[][name]" class="span5 dropdown">
			<% @criteria.each do |criterium| %>
				<option><%= criterium.name %></option>
			<% end %>
		</select>
	</div>
	<div class="span1"><%= number_field_tag 'criteria[][weight]', 30, :in => 0..100 %></div>
</div>

<div class="row">
	<div class="span5 offset1">
		<select name="criteria[][name]" class="span5 dropdown">
			<% @criteria.each do |criterium| %>
				<option><%= criterium.name %></option>
			<% end %>
		</select>
	</div>
	<div class="span1"><%= number_field_tag 'criteria[][weight]', 10, :in => 0..100 %></div>
</div>

<br/>
<h2>Make sure the three weights add up to 100 or this won't work as intended.</h2>

<!-- Only going to use top 3 criteria -->
<!-- <h1>2. Arrange these criteria in order of preference <small>more important ones at the top</small></h1>
<br/>
<div class="row">
	<div class="span6 offset1">
		<ul id="sortable">
			<%# @criteria.each do |criteria| %>
				<li class="pref-box ui-state-default span4"><%#= criteria.name %></li>
			<%# end %>
		</ul>
	</div>
</div> -->

<br/>

<!-- <h1>3. Pick your ideal price range</h1>	
<br/> -->
<div class="row">
	<!-- <div class="span3 offset1">
		<select class="span3 dropdown">
			<option>less than $500</option>
			<option>$500 to $800</option>
			<option>more than $800</option>
		</select>
	</div> -->
	<%= submit_tag("Tell me what to do!", class: "btn btn-primary btn-large pull-right") %>
	<!-- <span class="btn btn-primary btn-large pull-right" type="submit"><i class="icon-search icon-white"></i> Tell me what to do!</span> -->
</div>
<% end %>

<br/>

<script>
	$(function() {
		$( "#sortable" ).sortable({
			placeholder: "ui-state-highlight"
		});
		$( "#sortable" ).disableSelection();
	});
</script>